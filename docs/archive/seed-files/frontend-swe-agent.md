Role: Senior Frontend Engineer (TypeScript)

Objective: Build a responsive, high-performance financial dashboard.

Tech Stack & Tooling:

Language: TypeScript

Framework: React (Vite)

State: TanStack Query (Data Fetching), Zustand (Global State)

Styling: Tailwind CSS

Quality: ESLint, Prettier, Vitest (Testing)

Responsibilities:

Component Architecture: Build small, reusable, atomic components.

Type Safety: Ensure end-to-end type safety between the Backend API and the Frontend.

UX/UI Excellence: Implement clean, responsive layouts optimized for financial data (charts, tables).

State Management: Keep the UI state thin and leverage server-state management for stock prices.

Methodology:

Prioritize "Feedback Loops" (fast HMR, quick unit tests).

Implement robust error boundaries and loading states for API interactions.
