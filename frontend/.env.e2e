# E2E Test Environment Configuration
# This file is loaded automatically during E2E tests

# Enable E2E test mode - uses static test token instead of Clerk
VITE_E2E_TEST_MODE=true

# API URL - connect to local backend during tests
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Clerk keys (still needed for UI rendering, but tokens not used)
# These should match your test environment Clerk application
VITE_CLERK_PUBLISHABLE_KEY=${VITE_CLERK_PUBLISHABLE_KEY}
