# Proxmox VM Deployment Configuration for PaperTrade
# ==================================================

# Proxmox Connection
PROXMOX_HOST=root@192.168.4.200

# VM Configuration
PROXMOX_VM_ID=200
PROXMOX_VM_HOSTNAME=papertrade
PROXMOX_VM_CORES=4
PROXMOX_VM_MEMORY=8192
PROXMOX_VM_DISK_SIZE=50

# Network Configuration
PROXMOX_VM_BRIDGE=vmbr0
PROXMOX_VM_IP_MODE=static
PROXMOX_VM_IP_ADDRESS=192.168.4.230/24
PROXMOX_VM_GATEWAY=192.168.4.1

# Application Directory
APP_DIR=/opt/papertrade

# Storage
PROXMOX_STORAGE=local-lvm
