{
  "$schema": "https://modelcontextprotocol.io/schemas/mcp.json",
  "mcpServers": {
    "alphavantage": {
      "command": "bash",
      "args": [
        "-c",
        "set -a; [ -f .env ] && . ./.env; set +a; exec uvx --from marketdata-mcp-server marketdata-mcp \"$ALPHA_VANTAGE_API_KEY\""
      ]
    }
  }
}
